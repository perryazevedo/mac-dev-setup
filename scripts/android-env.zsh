# Android SDK paths (optional; Android Studio usually manages these)
export ANDROID_HOME="$HOME/Library/Android/sdk"
export PATH="$ANDROID_HOME/platform-tools:$ANDROID_HOME/emulator:$PATH"
# Ensure Gradle uses JDK 17 via Android Studio prefs.
